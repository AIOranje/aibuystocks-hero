import { JsonLogin } from "@/components/json-login"

export default function LoginPage() {
  return (
    <main className="min-h-screen" style={{ backgroundColor: "#0A0D18" }}>
      <JsonLogin />
    </main>
  )
}
